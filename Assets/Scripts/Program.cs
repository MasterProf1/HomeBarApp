using System;
using System.Collections.Generic;
using MyBarApp.Models;


namespace MyBarApp
{
    internal class Program
    {
        
        static void Main()
        {
            
            
           /* List<Ingridient> avaliableIngridients = new List<Ingridient>();
            avaliableIngridients.Add(ingridients[4]);
            avaliableIngridients.Add(ingridients[5]);

           var avaliableCoct = CocktailSearcher.GetListOfAvailableCocktails(avaliableIngridients, cocktails);
            foreach (var cocktail in avaliableCoct) 
            {
                Console.Write("Ви можете зробити: ");
                Console.WriteLine(cocktail.cocktailName);
            }*/

            Console.ReadLine();
        }
    }
    
}
